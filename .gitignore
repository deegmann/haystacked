# ── Secrets ──────────────────────────────────────────────────────────────────
.env
*.env.*

# ── Generated config (from generate_all.py via AP0 xlsx) ─────────────────────
config/field_levels.json
config/vehicle_types.json
config/scoring_weights.json
config/nace_codes.json
config/sqlite_schema.json
config/plausibility.json
config/ap0_checksum.txt
config/prompts/

# ── Airtable data — versioned in git for reproducible snapshots ───────────────
# data/raw/*.csv and data/haystacked.db are committed alongside each release tag.
# To rebuild the DB from committed CSVs without Airtable: python3 sync_airtable.py --local

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log
haystacked.log.*

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.venv/
venv/
env/

# ── Claude Code local session data ───────────────────────────────────────────
.claude/

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
**/.DS_Store

# ── Excel lock files & backups ────────────────────────────────────────────────
~$*.xlsx
~$*.xls
*.xlsx.bak_*

# ── Local research snapshots (DB is canonical) ────────────────────────────────
offline_supplier_research/
offline_supplier_research.zip

# ── Local design files ────────────────────────────────────────────────────────
design/
