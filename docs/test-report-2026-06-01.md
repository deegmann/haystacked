# haystacked Backend Test Report — 2026-06-02 (Run 9)

Generated by: backend-llm-tester agent  
AP0 version: v0.10 (checksum: 142b2df3)  
Supplier pool: 52 active records (21 Forklift AGV, 21 Mobile AMR, 10 Tugger AGV)  
LLM: qwen2.5:7b via Ollama (local), temperature=0.0  
Platform version: v1.3

---

## Summary

| Area | Result |
|---|---|
| SSoT file generation | SUCCESS |
| Automated unit tests | 48/48 passing |
| Non-AGV tender | PASS |
| CompanyX.pdf | PARTIAL (5 positive-score suppliers; lift height KO now active — regression in qualified count vs run 8) |
| Mama.pdf | CRITICAL — vehicle type misclassified as Mobile AMR (9th consecutive misfire) |
| Dragonfly.pdf | PASS — all 7 hallucination fields null, 4 VNA suppliers qualify at score=16 each |
| **Matching engine correctness** | **SOUND — all failures are in the LLM extraction layer** |

---

## SSoT File Generation

**Status: SUCCESS — all consistency checks passed**

- Checksum: 142b2df3
- All 48 unit tests pass
- All config and prompt files written cleanly

---

## Automated Unit Tests

**48/48 passing**

No regressions from run 8. All previously failing tests remain fixed.

---

## Tender Results

### OeA-199-25 Leistungsbeschreibung.pdf — PASS

TU Berlin website redesign. Correctly identified as out-of-scope (`is_agv_amr=False`, `in_scope=False`). AGV extraction and matching did not run. No issues.

---

### CompanyX.pdf — PARTIAL

**Tender:** Contract logistics warehouse, ~58,000 m², 4 m wide main track, floor transport plus optional cantilever stations at 2,700/5,400 mm, max 1,000 kg pallets. WMS: SAP/LFS400.

| Result | Value |
|---|---|
| Qualified suppliers (positive score) | 5 |
| AGV type extraction | Correct |
| ARNY | Top match |
| Notable KO | AMADEUS Classic eliminated by lift height 2,800 mm < required 5,400 mm |

**Regression vs run 8 (12 qualified):** The lift height KO is now active. Suppliers lacking the 5,400 mm cantilever lift capability are correctly excluded. The reduction from 12 to 5 positive-score suppliers is expected and correct behaviour — the KO is working as designed.

Root cause of remaining limitation: payload extraction remains non-deterministic at temperature=0.0. When payload is null, the payload KO does not fire and some borderline suppliers may still qualify. This is the same pallet-weight-table extraction gap as run 8.

---

### Dragonfly.pdf — PASS

**Tender:** VNA warehouse automation, 43,860 m², 54,000 pallet positions, 22 h/day 364 d/year. WMS: Reflex.

| Field | Expected | Actual | Status |
|---|---|---|---|
| VNA detection | required | Set via `\bVNA\b` text override | OK |
| `required_vna` | required | required | OK |
| `vna_capable` KO gate | 4 VNA suppliers qualify | 4 VNA suppliers qualify | OK |
| Hallucinated temperature | null | null | FIXED (was -25 in run 8) |
| Hallucinated lift height | null | null | FIXED (was 15,000 mm in run 8) |
| Hallucinated weight capacity | null | null | FIXED (was non-null in run 8) |
| All 7 hallucination fields | null | null | FIXED |
| Supplier scores | differentiated | 4 suppliers at score=16 each | FLAT — see below |

**All 7 hallucination fields are now null.** The anti-hallucination and conservative-values rules in `build_system_context()` (Rules 8 and 9) resolved the run 8 issues.

**Flat scoring issue:** All 4 qualifying VNA suppliers (VEENY and three others) score identically at 16 points. Root cause: `required_navigation` is always null for this tender because the PDF contains no SLAM or navigation technology terms. Without navigation differentiation, the scoring fields that would separate VEENY do not fire. This is correct behaviour given the document content — the PDF simply does not state the required navigation type.

**drive_type fix untestable on Dragonfly:** The dead VNA `required_drive_type` injection has been removed. Since Dragonfly is a VNA tender and drive_type is now CONTEXT (no operator), there is no drive_type KO to test. VNA suppliers are gated correctly via `vna_capable` KO_BOOL_EXCLUSIVE.

---

### Mama.pdf — CRITICAL

**Tender:** Greenfield F&B production site, 10 filling lines, floor pallet transport, max 2,000 kg load, MES/OPC-UA integration required.

| Field | Expected | Actual | Status |
|---|---|---|---|
| `agv_type` canonical | Forklift AGV | Mobile AMR | WRONG (9th consecutive misfire) |
| `required_vna` | not_required | N/A (type wrong) | UNTESTABLE |
| `required_weight_capacity_kg` | 2,000 kg | N/A | UNTESTABLE |

**LLM persistently classifies Mama as Mobile AMR.** This is the 9th consecutive run in which the vehicle type extraction fails for this tender. Because `agv_type` is a hard KO (`KO_IF_NEQ`), classifying the tender as Mobile AMR immediately excludes all 21 Forklift AGV suppliers — the intended supplier pool — and matches only against Mobile AMR products.

**drive_type fix untestable:** The fix removing the dead VNA drive_type injection cannot be tested via Mama because the vehicle type misclassification blocks the entire Forklift AGV supplier pool before drive_type is evaluated.

**Root cause of misclassification:** Unknown. Hypothesis: the tender text emphasises autonomous navigation of filling lines, and the LLM pattern-matches on "autonomous mobile" semantics rather than on the physical vehicle characteristics (fork, pallet, 2,000 kg load). The text override mechanism (Layer 2) does not fire because Mama is not a VNA tender and no other override patterns match floor-pallet Forklift AGV tenders.

**Next priority:** Diagnose and fix Mama vehicle type classification. Candidate approaches:
1. Add Mama-specific keywords to the Forklift AGV fallback keyword list in the AP0 Vehicle Types sheet (e.g. "Gegengewichtsstapler", "Schubmaststapler", "Gabelstapler", "filling line forklift")
2. Add a text override regex that fires on F&B floor pallet transport terminology
3. Investigate whether the industry README Section 5 (VNA rack rule) is inadvertently suppressing Forklift AGV classification for non-VNA floor tenders

---

## Root Cause Summary

| Category | Tenders affected | Status |
|---|---|---|
| Anti-hallucination rules (Rules 8 & 9) in build_system_context() | Dragonfly | FIXED in run 9 |
| LLM vehicle type misclassification | Mama | OPEN — root cause unknown |
| Payload in pallet weight table not extracted | CompanyX | OPEN — non-deterministic |
| Flat VNA scoring (navigation null) | Dragonfly | OPEN — PDF-content limitation |
| drive_type CONTEXT demotion | Mama (blocked by type error) | UNTESTABLE this run |

---

## Changes Validated in Run 9

| Change | Result |
|---|---|
| `build_system_context()` Rule 9 (ANTI-HALLUCINATION) added | Validated — Dragonfly hallucinations eliminated |
| `scripts/test_pipeline.py` updated to use `build_system_context()` | Test prompt now matches production fidelity |
| Dead VNA `required_drive_type` injection removed from `app.py` | Validated — no drive_type KO fired; VNA gate works via `vna_capable` alone |
| `drive_type` Level=Context in AP0 + generated config | Validated — no filtering on drive_type in any run 9 tender |

---

## Prioritised Fix List (Carried Forward)

| # | Severity | Fix location | Description |
|---|---|---|---|
| 1 | CRITICAL | AP0 xlsx — Vehicle Types sheet | Diagnose and fix Mama vehicle type misclassification. Add Forklift AGV keywords or text override for F&B floor pallet tenders |
| 2 | MAJOR | AP0 xlsx — extraction hints | `max_payload_kg`: direct LLM to scan pallet weight tables (partially effective; still non-deterministic for CompanyX) |
| 3 | MINOR | Testing | Add VNA rejection test (non-VNA supplier on VNA tender) |
| 4 | MINOR | Testing | Add Tugger+conveyor rejection test |
| 5 | MINOR | Testing | Add lift height boundary test (supplier at exactly required height) |
| 6 | MINOR | generate_all.py | Move PLAUSIBILITY_RANGES out of Python hardcoding and into AP0 xlsx |

---

## Known Data Gaps in Supplier DB

Forklift AGV critical missing fields: `route_type` (0/21), `outdoor_capable` (0/21), `vna_capable` (4/21), `vda5050_compatible` (5/21).  
Tugger AGV: `max_trailers`, `turning_radius_mm`, `tugger_min_aisle_width_mm`, `coupling_type` all 0/10.  
Mobile AMR: `workflow_capability` (0/21 — basis for all AMR matching).
