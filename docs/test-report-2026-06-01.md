# haystacked Backend Test Report — 2026-06-01

Generated by: backend-llm-tester agent + ap0-architecture-guardian agent  
AP0 version: v0.10 (checksum: 9a218013)  
Supplier pool: 52 active records (21 Forklift AGV, 21 Mobile AMR, 10 Tugger AGV)  
LLM: qwen2.5:7b via Ollama (local)

---

## SSoT File Generation

**Status: SUCCESS — all consistency checks passed**

- 117 fields processed (19 KO, 33 COND_KO)
- 13 vehicle type mappings, 8 with LLM guide
- 58 NACE Prio-1 codes, 51 extraction fields, 7 plausibility ranges
- All 17 config/prompt files written cleanly

---

## Automated Test Suite

**47/48 passing (97.9%)**

**FAILED: `test_U_K_06_goods_to_person`**
- Assertion: `agv_type_keyword_fallback("Goods-to-Person picking system needed.") == "Mobile AMR"`
- Result: returns `None`
- Root cause: "goods-to-person" is missing from the Mobile AMR `keyword_map` in the AP0 xlsx Vehicle Types sheet ("Fallback Keywords" column)
- Fix: Add `goods-to-person|goods to person` to the Mobile AMR keyword row in AP0 xlsx, then run `generate_all.py`

---

## Tender Results

### OeA-199-25 Leistungsbeschreibung.pdf — ✅ PASS
TU Berlin website redesign. Correctly identified as out-of-scope (`is_agv_amr=False`, `in_scope=False`). No matching ran. No issues.

---

### Dragonfly.pdf — ⚠️ PARTIAL

**Tender:** VNA warehouse automation, 43,860 m², 54,000 pallet positions, 22h/day 364d/year. WMS: Reflex.

| Field | Expected | Actual | Status |
|---|---|---|---|
| `is_agv_amr` | True | True | ✅ |
| `in_scope` | True | True | ✅ |
| VNA detection | required | ✅ Set via `\bVNA\b` text override | ✅ |
| `required_drive_type` | VNA Turret | ✅ Set via app.py override | ✅ |
| `required_weight_capacity_kg` | 1000 kg | ❌ null (LLM extracted 29kg tare; plausibility filter killed it; 1000kg max load from pallet table not extracted) | ❌ MINOR |
| `required_navigation` | stable | ❌ Non-deterministic: Run 1 → null (4 VNA suppliers), Run 2 → SLAM (1 supplier) | ❌ MAJOR |

**Mismatch detail — LLM non-determinism:**  
Two runs at identical settings (temperature=0.05) produced qualitatively different results. With `navigation=null`: VEENY, EKX 516a, E80 Trilateral, MX-X iGo all matched (score=16 each). With `navigation=SLAM`: only VEENY matched (score=1). The SLAM run is the more correct result, but reproducibility is broken. Temperature 0.05 is insufficient for consistent extraction with qwen2.5:7b on complex tender documents.

---

### Mama.pdf — ⚠️ PARTIAL

**Tender:** Greenfield F&B production site, 10 filling lines, floor pallet transport, max 2000 kg load, MES/OPC-UA integration required.

| Field | Expected | Actual | Status |
|---|---|---|---|
| `is_agv_amr` | True | True | ✅ |
| `in_scope` | True | True | ✅ |
| `agv_type` canonical | Forklift AGV | Forklift AGV | ✅ |
| `required_vna` | not_required | not_required | ✅ VNA suppliers correctly excluded |
| `required_weight_capacity_kg` | 2000 kg | 2000 kg | ✅ |
| `required_drive_type` | not stated in tender | ❌ LLM extracted `Counterbalanced` → hard KO applied | ❌ MAJOR |

**Mismatch detail — drive_type hard KO over-excludes:**  
The tender does not specify drive type — it specifies floor-to-floor pallet transport at 2000kg. LLM extracted `required_drive_type=Counterbalanced` as a hard KO (level=KO in field_levels.json), which eliminated:
- FM-X iGo (STILL, Reach Truck, 2500kg) — legitimately capable
- ERC 213a (Jungheinrich, Reach Truck) — legitimately capable  
- E80 Reach LGV (3000kg, Reach Truck) — legitimately capable

Actual top match: ARNY (DS Automotion, score=22). Jungheinrich and STILL — the tender's preferred suppliers — were excluded by the drive_type KO.

**Fix options (in order of preference):**
1. Demote `drive_type` from KO → COND_KO in AP0 xlsx
2. Tighten extraction prompt: only extract `required_drive_type` if the tender explicitly states it

---

### CompanyX.pdf — ❌ CRITICAL MISMATCH

**Tender:** Contract logistics warehouse, ~58,000 m², 4m wide main track, floor transport + optional cantilever stations at 2700/5400mm, max 1000 kg pallets. WMS: SAP/LFS400.

| Field | Expected | Actual | Status |
|---|---|---|---|
| `is_agv_amr` | True | True | ✅ |
| `in_scope` | True | True | ✅ (NACE 28.99 correct) |
| `agv_type` canonical | Forklift AGV | ❌ null — LLM returned generic `"AGV"`, not in `vt_map` | ❌ CRITICAL |
| `required_vna` | not_required | ❌ null — cascades from missing canonical | ❌ MAJOR |
| `required_weight_capacity_kg` | 1000 kg | ❌ null — not extracted | ❌ MAJOR |
| Suppliers qualified | ~15–20 Forklift AGV | ❌ ALL 52 (no agv_type KO fired) | ❌ CRITICAL |

**Mismatch detail — cascading null failure:**  
The LLM returned `required_vehicle_type = "AGV"` (generic). `"agv"` is absent from the `vt_map` in `vehicle_types.json`. This triggers a cascade:
1. `canonical_agv_type = null` → agv_type KO_IF_NEQ does not fire → Tuggers, AMRs, VNA forklifts all remain in pool
2. app.py VNA logic: `required_vna = "not_required" if canonical == "Forklift AGV" else None` → evaluates to `None` → KO_BOOL_EXCLUSIVE does not activate → VNA suppliers not excluded
3. Payload null → no payload KO

Result: All 52 suppliers appear as qualified candidates including G2P AMRs (MiR250 at 250kg), Tugger AGVs, and VNA forklifts (wholly inappropriate for a 4m-wide-aisle standard pallet tender).

**Fix:** Add `"agv": "Forklift AGV"` to the Vehicle Types sheet in AP0 xlsx (`vt_map`). Run `generate_all.py`.

---

## Prioritised Fix List

| # | Severity | Fix location | Description |
|---|---|---|---|
| 1 | **CRITICAL** | AP0 xlsx — Vehicle Types sheet | Add `"agv"` → `"Forklift AGV"` to vt_map. Prevents the null canonical cascade that disables all KOs. |
| 2 | **MAJOR** | AP0 xlsx — SHARED sheet | Demote `drive_type` from KO → COND_KO. Prevents LLM over-extraction from wrongly excluding Reach Truck suppliers. |
| 3 | **MAJOR** | Ollama config (`app.py` line ~277) | Lower temperature from 0.05 → 0.0 (or try `temperature: 0`) for qwen2.5:7b. Reduces navigation field non-determinism. |
| 4 | **MAJOR** | AP0 xlsx — extraction Description column | Strengthen payload extraction hint for tabular pallet data. Both Dragonfly and CompanyX missed 1000kg from clearly-stated pallet weight tables. |
| 5 | **MINOR** | AP0 xlsx — Vehicle Types sheet | Add `goods-to-person\|goods to person` to Mobile AMR fallback keywords. Fixes `test_U_K_06`. |

---

## AP0 Architecture Violations (from ap0-architecture-guardian)

Six hardcoded domain-logic violations found in Python code — none should require a code fix, all should come from AP0/config:

| ID | Severity | File | Lines | Issue |
|---|---|---|---|---|
| C-1 | CRITICAL | `app.py` | 385–388 | Duplicate AGV detection keyword list (diverges from `vehicle_types.json`) |
| C-2 | CRITICAL | `src/matching.py` | 351–353 | Hardcoded type→scoring bucket dict `{"Forklift AGV": "forklift_specific", ...}` |
| C-3 | CRITICAL | `src/matching.py` | 458–493 | `if/elif agv_t ==` scoring branches — entire block is industry knowledge in Python |
| C-4 | CRITICAL | `src/matching.py` | 423–501 | Hardcoded numeric scoring thresholds (8h battery, 26 weeks, 300 picks/hr, bonus literals) |
| C-5 | CRITICAL | `app.py` | 548–550 | `"Forklift AGV"` hardcoded as VNA-applicability gate |
| C-6 | HIGH | `sync_airtable.py` | 362–400 | 80-field hardcoded column list that drifts silently from AP0 |

Logic bug: M-1 — "Schmalgangstapler" keyword maps to `Forklift AGV` (correct) but does NOT set `is_vna_subtype=True` → results in `required_vna="not_required"` for a VNA tender (inverted).

Missing tests: VNA required/excluded, lift height boundary (5999mm vs 6000mm), Tugger rejected for Conveyor tender, `test_U_M_14` is a vacuous assertion (`isinstance(x, bool)`), JSON repair tests cover `src/llm_client.py` not the production `app.py` implementation.

---

## Known Data Gaps in Supplier DB (from Spec/TODO.md)

Forklift AGV critical missing fields: `route_type` (0/21), `outdoor_capable` (0/21), `vna_capable` (4/21), `vda5050_compatible` (5/21).  
Tugger AGV: `max_trailers`, `turning_radius_mm`, `tugger_min_aisle_width_mm`, `coupling_type` all 0/10.  
Mobile AMR: `workflow_capability` (0/21 — basis for all AMR matching).
